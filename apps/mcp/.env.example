# Payer account: a stellar-cli identity (preferred) or a raw testnet secret key
STELLAR_IDENTITY=local402-agent
# STELLAR_PRIVATE_KEY=S...
# Default payment asset: USDC (stock exact scheme) or XLM (exact-fx, swapped to USDC by FxPay)
PAY_WITH=XLM
# Refuse any single payment above this local price
MAX_PRICE=500 CLP
